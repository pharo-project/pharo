"
- shared slots are not well tested.
- the metaclass should probably know it instance side and delegate may of the superclass api such as the call to is* 
- support for layout: 
- Can and should we parse
Trait named: #TAssertable
     uses: {}
     category: 'SUnit-Core-Traits'
and class side
TAssertable classTrait uses: {}

"
Class {
	#name : #CDTodo,
	#superclass : #Object,
	#category : #'ClassParser-Model'
}
