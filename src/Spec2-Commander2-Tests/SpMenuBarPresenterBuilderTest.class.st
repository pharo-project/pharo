Class {
	#name : #SpMenuBarPresenterBuilderTest,
	#superclass : #SpMenuPresenterBuilderTest,
	#category : #'Spec2-Commander2-Tests'
}

{ #category : #running }
SpMenuBarPresenterBuilderTest >> setUp [
	super setUp.
	menuBuilder := SpMenuBarPresenterBuilder new.
]
