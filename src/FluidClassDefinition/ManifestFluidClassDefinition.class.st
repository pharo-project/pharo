"
This package contains:
- a little class builder so that we can create a new class without parsing,
- a class definition parser that creates an AST tree. 
"
Class {
	#name : #ManifestFluidClassDefinition,
	#superclass : #PackageManifest,
	#category : #'FluidClassDefinition-Manifest'
}
