"
I model an abstract interaction error.

These errors are raised when an interaction with a user did not end well.
"
Class {
	#name : #SpInteractionError,
	#superclass : #Error,
	#category : #'Spec2-Interactions'
}
