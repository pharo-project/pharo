Class {
	#name : #AbstractReporterTest,
	#superclass : #TestCase,
	#instVars : [
		'reporter'
	],
	#category : #'TestRunner-Tests-Reporters'
}

{ #category : #accessing }
AbstractReporterTest >> reporter [

	^ reporter
]
