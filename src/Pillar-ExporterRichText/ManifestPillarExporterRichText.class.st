"
This package is generating Pillar objects to rich text. For the moment only a subset is taking into account. 
It is done in the class `PRRichTextComposer`, which at the class side have several examples.

The pillar is rendered into a `Text` object with `TextAttribute`'s.


"
Class {
	#name : #ManifestPillarExporterRichText,
	#superclass : #PackageManifest,
	#category : #'Pillar-ExporterRichText-Manifest'
}
