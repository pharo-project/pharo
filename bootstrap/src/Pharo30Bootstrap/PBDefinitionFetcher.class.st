"
Base class used to fetch Monticello definitions (classes, methods) that will be used to create a boostraped image
"
Class {
	#name : #PBDefinitionFetcher,
	#superclass : #Object,
	#category : 'Pharo30Bootstrap-Deprecated'
}

{ #category : #versioning }
PBDefinitionFetcher >> majorVersion [

	^ nil
]

{ #category : #versioning }
PBDefinitionFetcher >> minorVersion [

	^ nil
]

{ #category : #versioning }
PBDefinitionFetcher >> updateNumber [

	^ nil
]
